package functional

// DO NOT EDIT.
// Code generated by cmd/generate.go
// source: reduce.tmpl

{{range $idx,$typ :=.types}}
// Reduce{{$typ|firstToUpper}} ...
func Reduce{{$typ|firstToUpper}}(slice []{{$typ}}, fn func(x {{$typ}}, y {{$typ}}) {{$typ}}) (ret {{$typ}}) {
	if len(slice) < 2 {
		panic("reduce: slice too short")
	}
	ret = slice[0]
    for i := 1; i < len(slice); i++ {
        ret = fn(ret, slice[i])
    }
    return
}
{{end}}
